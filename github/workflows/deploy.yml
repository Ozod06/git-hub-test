name: Deploy to Netlify

on:
  push:
    branches:
      - main   # faqat main branch ga push bo‘lganda ishlaydi

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Netlify
        uses: nwtgck/actions-netlify@v2.0.0
        with:
          publish-dir: ./
          production-deploy: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ nfp_cNFoa6An48nnJpAZRNzAFSrvhoxLGUKza841 }}
          NETLIFY_SITE_ID: ${{ 0e92a425-0b0b-43de-b24b-797bc2c641f3}}
